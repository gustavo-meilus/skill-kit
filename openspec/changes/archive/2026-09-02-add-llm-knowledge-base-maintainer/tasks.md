## 1. Skill Package

- [x] 1.1 Create the repository-native `llm-knowledge-base-maintainer` plugin manifests, skill directory, and Codex `agents/openai.yaml`; verify every JSON and YAML metadata file parses and all declared skill paths resolve.
- [x] 1.2 Write the focused `SKILL.md` workflow and one conditionally loaded default-layout reference covering canonical Markdown, stable identity, `llms.txt`, `manifest.jsonl`, evidence handling, preservation, and integrity checks; verify `skill-creator`'s `quick_validate.py` passes and no placeholder remains.
- [x] 1.3 Review the package against the approved specification, `skill-creator`, `writing-for-agents`, and repository authoring policy; verify each required behavior is represented once without adding scripts, hooks, custom agents, MCP servers, runtime dependencies, or default `llms-full.txt` output.

## 2. Distribution

- [x] 2.1 Append consistent `llm-knowledge-base-maintainer` entries to the Codex and Claude marketplace manifests; verify both catalogs parse and the repository marketplace integrity test resolves the new plugin package.
- [x] 2.2 Add the skill to README discovery and install guidance and update host-support package counts or examples where applicable; verify names, descriptions, entry points, and support claims agree with the checked-in manifests.

## 3. Behavioral Verification

- [x] 3.1 Forward-test the skill in isolated temporary collections for new creation, single-page revision with stable identity, expansion with relevant cross-links, authorized removal, existing-convention reuse, conflict handling, invalid required metadata, and an unrepaired integrity defect; verify the resulting references and both static indexes satisfy the corresponding scenarios.
- [x] 3.2 Forward-test invocation boundaries with knowledge-base maintenance and adjacent prose, research, website, and retrieval-infrastructure prompts; verify the model-invoked description selects only the intended requests.
- [x] 3.3 Run the focused skill, plugin, marketplace, and metadata checks, then use a fresh independent Verifier to review behavior and package coherence; verify all checks and the final verdict pass or complete one bounded rework cycle.
